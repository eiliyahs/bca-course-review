<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/stylesheets/review.css">
    
    <title>Details</title>
</head>
<body>
    <div class="container">
        <a href="/review/"><button type="button" class="btn btn-primary"><i class="fa-solid fa-arrow-left"></i></button></a>

        <h1><%= elective.elective_name %></h1>
        <p>Description: <%= elective.description %></p>
        <p>Teachers: <%= elective.teacher %></p>
        <p>Duration: <%= elective.duration %></p>
        <p>Goes to GPA? <p><%= elective.gpa === 1 ? "Yes" : "No" %></p></p>
        <p>Art Elective? <p><%= elective.art === 1 ? "Yes" : "No" %></p></p>


        <h2>Comments</h2>
        <form method="post" action="/review/<%= electiveId %>">
            <div class="form-group">
                <input name="name" type="name" value="<%= comments.comment_user %>" class="form-control" id="name" placeholder="Enter name">
            </div>
            <div class="form-group">
                <textarea name="comment" class="form-control" value="<%= comments.comment %>" placeholder="Enter comment" id="comment" rows="3"></textarea>
            </div>
            
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
        
        
        <% comments.forEach((comment) => { %>
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title"><%= comment.comment_user %></h5>
                        <p class="card-text"><%= comment.comment %></p>
                    </div>
                </div>
        <% }) %>
    </div>
    
</body>
</html>